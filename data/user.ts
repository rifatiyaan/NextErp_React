import type { UserType } from "@/types"

export const userData: UserType = {
    id: "u1",
    name: "John Doe",
    email: "john.doe@example.com",
    avatar: "", // Placeholder or use a real URL if allowed
    role: "Admin",
}
